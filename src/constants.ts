export const KETO_NAMESPACES = Object.freeze({
  permission: 'permission',
  role: 'role'
})

export const KETO_RELATIONS = Object.freeze({
  excludes: 'excludes',
  granted: 'granted',
  member: 'member'
})

export const PAGE_SIZE = 1000000
